open Dynjit.Ccall

let () =
  let _ = pyinit() in
  let o = pyDict_New() in
  let none = pyeval "None" in
  let ff = pyeval "lambda : print(996)" in
  let _ = pycall ff none in
  let _ = incref o in
  let _ = print_endline "oo" in
  (*  let _ = _bytes2str (_pybytes o) in *)
  let _ = print_string "xxxx" in
  ()

